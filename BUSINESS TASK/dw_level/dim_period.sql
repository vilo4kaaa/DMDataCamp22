alter session set current_schema = DW_DATA;

CREATE TABLE DIM_PERIOD(
    PERIOD_ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY,     
    VALID_FROM               DATE,
    VALID_TO                 DATE,
    CONSTRAINT "PK_T.DIM_PERIOD" PRIMARY KEY(PERIOD_ID) 
);